ja:
  activerecord:
    errors:
      models:
        post:
          attributes:
            base: 
              validate_content_attachment_byte_size: 添付ファイルは%{:max_content_attachment_mega_byte_size}MB以下にしてください。 [%{byte}/%{max_bytes}]
            title:
              blank: が空です。
              too_long: が%{count}文字を超えています。
            content:
              too_long: が%{max_content_length}文字を超えています。 [%{length}/%{max_content_length}]
    attributes:
      post:
        title: タイトル
        content: コンテンツ